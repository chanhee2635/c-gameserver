# 신입 게임 서버 개발자 포트폴리오 가이드

> 서류 패스 + 기술 면접을 한 번에 통과하기 위한 포트폴리오 청사진.
> 핵심 전략: **웹 API 서버(인프라/재화) + 분산 소켓 서버(실시간 인게임)** 를 결합한 **하이브리드(Hybrid) 아키텍처**.
> 국내 메이저 게임사(넥슨, 넷마블, 엔씨, 크래프톤, 카카오게임즈 등) 상용 게임 90% 이상이 채택하는 구조이므로,
> "실무 투입 즉시 아키텍처를 이해하고 코드를 짤 수 있는 신입"이라는 인상을 준다.

> 📎 **관련 문서:** 이동/공격 패킷 파이프라인과 AOI 단계별 최적화(O(N²) 방어, 인원 제한, Hysteresis, 틱 배칭, 격자 직렬화 공유, 우선순위 대역폭 제어)는 [`AOI_MOVEMENT_PIPELINE.md`](./AOI_MOVEMENT_PIPELINE.md) 참고.

---

## 1. 하이브리드 서버 아키텍처

```
[ Client (Unity / Unreal) ]
     │
     ├── (HTTP / REST) ──► [ 1. 인증/재화 웹 서버 ] ──► [ MySQL (RDBMS) ]
     │                                │ (인증 토큰 저장)
     │                                ▼
     │                         [ 3. 공유 캐시 (Redis) ]
     │                                ▲
     │                                │ (인증 토큰 검증)
     └── (TCP / Socket) ──► [ 2. 실시간 동기화 서버 ]
```

### 1) 인증 및 재화 처리용 웹 서버 (ASP.NET Core 또는 Node.js)

모바일 게임 콘텐츠의 70% 이상(상점, 가챠, 우편함, 출석 체크 등)은 실시간 소켓 동기화가 불필요하다.
오히려 웹의 **안정적인 트랜잭션 처리**가 중요하다.

- **핵심 역할:** 로그인, JWT 인증 토큰 발급, 상점 구매 및 가챠(뽑기) 로직 처리
- **데이터베이스:** RDBMS(MySQL 등) 연동
- **신입의 엣지(Edge):**
  - 가챠 버튼 연타 등의 어뷰징을 막기 위한 **Redis 분산 락(Redlock)** 구현
  - 또는 DB 트랜잭션 격리 수준(Serializable 등)을 고려한 데이터 무결성 보장
  - **돈과 직결된 재화 로직을 얼마나 엄격하게 짰는지**가 핵심

### 2) 인게임 실시간 동기화 서버 (C++ IOCP 또는 C# Netty/epoll)

실시간 이동, 전투, 스킬, 채팅 등 0.1초의 지연도 용납되지 않는 인게임 전용 소켓 서버.

- **핵심 역할:** 패킷 수신 및 브로드캐스팅, 몬스터 AI, 이동 동기화
- **스레드 모델:** `DoWorkerJob` + 분산 JobQueue (Zone 또는 Room 단위)
- **신입의 엣지(Edge):**
  - 데드락(Deadlock) 위험을 원천 차단하기 위해 **특정 영역(Zone/Room)을 한 번에 하나의 스레드만 점유**하여 실행하는 락 프리(Lock-Free) 컨텐츠 루프 설계
  - 면접관이 가장 깊게 파고들고 감탄하는 포인트

### 3) 데이터 공유 및 캐시 레이어 (Redis)

웹 서버와 소켓 서버 사이의 **징검다리** 역할.

- **핵심 역할:**
  - 웹 서버가 발급한 인증 토큰을 저장 → 소켓 서버가 접속 시 토큰을 조회해 정당한 유저인지 검증(**세션 공유**)
  - 실시간 전 서버 랭킹 시스템(Sorted Set) 구현

---

## 2. 포트폴리오의 '격'을 바꾸는 필수 디테일 3가지

### ① 상용 수준의 데이터 직렬화 및 최적화

- **Google Protocol Buffers(Protobuf)** 또는 **FlatBuffers** 연동
  - 실무에서 쌩 바이트 버퍼 파싱을 직접 짜는 경우는 거의 없음
  - 외부 직렬화 라이브러리를 빌드 파이프라인에 얹어본 경험 자체가 가산점
- **SendBuffer Pooling & 모아쏘기(Flush):**
  - 매 패킷마다 `WSASend` 시스템 콜을 날리는 대신, 커스텀 메모리 풀에서 버퍼를 할당받아 **프레임별로 모아 한 번에 전송**

### ② DB 쓰기 지연 (Write-Back) 구조

- 인게임에서 경험치/아이템이 변할 때마다 MySQL에 `UPDATE`를 날리면 DB가 먼저 터진다.
- 데이터 변동은 **메모리(혹은 Redis)에서 즉시 처리**, **5~10분 주기 또는 로그아웃 시점에만** RDBMS에 비동기로 몰아서 저장하는 쓰기 지연 스케줄러 구현
- 시스템 병목을 이해하고 있다는 강력한 증거

### ③ ★★★ 데이터로 증명하는 더미 클라이언트 툴 (합격 치트키)

> "제 서버는 좋습니다"라는 백 마디 말보다 "더미 3,000개 붙여서 검증했습니다"라는 그래프 한 장이 수백 배 강력하다.

- C# 콘솔 또는 고성능 비동기 소켓으로 **수천 개의 가상 유저를 동시 접속**시키는 더미 클라이언트 제작
- 이동/전투 패킷을 마구 쏘게 한 뒤 GitHub README에 지표 기재

**[스트레스 테스트 결과 수치 예시]**

| 항목 | 수치 |
|---|---|
| 동시 접속자 수 | 3,000 더미 클라이언트 |
| 초당 패킷 처리량(TPS) | 약 30,000 Packet/sec |
| 서버 CPU 점유율 | 평균 24% 유지 |
| 패킷 유실률 | 0% |
| 평균 패킷 응답 처리 속도 | 5ms 미만 |

---

## 3. 더미 클라이언트 핵심 테스트 유형 4가지

### 🏃 1. 이동 및 브로드캐스팅 폭풍 테스트 (가장 중요)

`ZoneWorld` 구조의 성능을 검증하는 핵심 테스트.

- **방법:** 3,000개 더미를 하나의 Zone에 몰아넣거나 여러 Zone에 분산시킨 뒤, 초당 10번씩 무작위 좌표로 `C_Move` 패킷을 동시 전송
- **서버가 할 일:** 초당 30,000개의 이동 패킷을 받아 JobQueue에 넣고, 처리 후 주변 유저에게 `S_Move` 브로드캐스팅
- **체크 포인트:**
  - 한 격자에 3,000명이 모이면 브로드캐스팅 연산량은 `3,000 × 3,000 = 9,000,000`번으로 폭증
  - "CPU 100% 치며 패킷이 밀리는가? Bandwidth 제한이나 SendBuffer 풀이 버티는가?"
  - **AOI(Area of Interest) 알고리즘이 왜 필요한지 데이터로 증명**

### ⚔️ 2. 시나리오 기반 콘텐츠 로직 테스트 (콘텐츠 루프 검증)

JobQueue 독점 구조와 스레드 안전성 검증.

- **방법:** 더미에 상태 머신(FSM)을 심어 인게임 루프 실행
  ```
  로그인 성공 ──► 사냥터 이동 ──► 주변 몬스터 탐색 ──► 초당 2회 기본 공격
    ──► 몬스터 사망 시 루팅/경험치 획득 ──► 체력 떨어지면 포션 사용 ──► 반복
  ```
- **서버가 할 일:** 3,000개 더미가 동시에 데미지 계산, HP 가감, 아이템 드랍, 레벨업 수행
- **체크 포인트:**
  - 데이터 오염(Race Condition)이나 크래시 발생 여부
  - 락 설계가 잘못되면 시작 1분 안에 Deadlock으로 굳거나 몬스터 HP가 음수로 떨어짐
  - "3,000명이 사냥해도 JobQueue 구조 덕분에 크래시 없이 완벽 동기화" 증명

### 🔐 3. 로그인 / 로그아웃 대량 병목 테스트 (인프라 검증)

게임 오픈 당일 "로그인 게이트 폭발" 상황 재현.

- **방법:** 3,000개 더미가 1~2초 이내에 동시 커넥션 + 인증 패킷 연사, 반대로 동시 로그아웃
- **서버가 할 일:** Accept 스레드가 세션 생성 → Redis/웹 서버로 JWT 검증 → MySQL에서 캐릭터 로딩 → 로그아웃 시 Write-Back
- **체크 포인트:**
  - 순간 대량 Accept를 IOCP가 밀리지 않고 처리하는가?
  - DB 커넥션 풀이 모자라 타임아웃이 뜨지 않는가?
  - **순간 초당 접속자 수(CPS) 한계 측정**

### ⏰ 4. 장시간 방치 테스트 (메모리 누수 검증)

완성도를 '상용 제품' 수준으로 끌어올리는 백그라운드 테스트.

- **방법:** 1, 2번 테스트(이동+전투)를 켜둔 채 3~6시간 이상 서버 방치
- **서버가 할 일:** 끊임없이 생성/소멸하는 패킷 버퍼, 세션, Job 객체 처리
- **체크 포인트 (메모리 그래프 관찰):**
  - ❌ 나쁜 예: 시간이 지날수록 우상향하며 계속 증가 (delete 누락 / 스마트 포인터 순환 참조 → 메모리 누수)
  - ✅ 좋은 예: 초반에 오브젝트 풀 크기만큼 차오른 뒤 수평선(Plateau) 유지
- "커스텀 메모리 풀과 스마트 포인터 관리로 6시간 연속 하드 테스트에도 메모리 누수 0B" 한 줄은 엄청난 신뢰감

---

## 4. 포트폴리오에 적어야 할 '진짜 데이터' 양식

```markdown
### 📊 대규모 스트레스 테스트 결과
- 테스트 환경: 단일 로컬 PC (AMD Ryzen 7, 32GB RAM)에서 서버 및 더미 3,000개 동시 구동
- 더미 시나리오: 3,000개 클라이언트가 4개 Zone에 분산 배치, 초당 10회 이동 + 초당 2회 공격
- 최대 TPS: 평균 36,000 packet/sec 소화
- CPU 및 메모리: CPU 평균 28%, 메모리 450MB에서 스파이크 없이 수평 유지
- 평균 패킷 처리 응답 시간: 3.2ms (수신 → 로직 처리 → 브로드캐스팅 완료까지)
- 발견 및 개선 사항: 초기 1,500명 부근에서 WSASend 실패(WSAENOBUFS) 발생
  → SendBuffer 프레임별 모아쏘기(Flush) 구조로 개선하여 3,000명 안정 수용 성공
```

> 면접관은 코드 몇 줄보다, **테스트를 설계해 서버의 한계를 측정하고 병목을 만나 직접 수정해 본 트러블슈팅 경험**을 훨씬 높게 평가한다.
> 더미 클라이언트는 선택이 아니라 **필수 무기**다.

---

## 5. 더미 테스트 시 지형/장애물(Collision) 처리

"서버가 지형 정보를 들고 있느냐"에 따라 현실적인 선택지가 나뉜다.

### 🗺️ 방법 1. [가장 추천] 서버가 격자(Grid) 맵 데이터를 들고 검증

Zone 분산/Grid 아키텍처라면 가장 정석적이고 면접에서 할 말이 많아진다.

- **구현:** Unity/Unreal 맵을 기반으로 이동 가능(0)/장애물(1)을 구분한 가벼운 2차원 배열(Txt/JSON) 추출 → 서버와 더미 모두 이 `MapFile` 로딩
- **더미 행동:** 무작위 이동 시 맵 데이터를 확인해 `0`(이동 가능)으로만 타겟 좌표 생성. 또는 간단한 A*(A-Star) 길찾기를 더미에 심어 장애물 회피
- **서버 검증(핵심):** `C_Move` 수신 시 서버도 맵 데이터를 대조해 "벽을 뚫고 순간이동했는가?"를 검증(`IsNavigable`)
- **💡 면접 어필 멘트:**
  > "서버가 완전히 독립적으로 클라이언트의 무결성을 검증해야 한다고 생각했습니다. Unity 맵을 격자 데이터로 변환해 서버에 올렸고, 더미가 쏜 이동 패킷이 장애물을 관통하는지 서버 측에서 validation하는 로직을 포함해 테스트했습니다."
  - → **핵(Hack) 방어 개념**이 있는 지원자로 눈도장

### 🪨 방법 2. [효율성 중심] 장애물 없는 '광활한 평지'에서 테스트

지형 검증(NavMesh/Grid) 구현이 시간적으로 부담되면, 테스트 맵을 장애물 없는 평야(콜로세움/광장)로 세팅. 실무에서도 자주 쓰임.

- **구현:** 1000×1000 크기의 장애물 없는 대형 맵
- **더미 행동:** 범위(MinX~MaxX, MinZ~MaxZ) 안에서 덧셈/뺄셈만으로 랜덤 배회 → 길찾기 연산이 없어 더미 자체 부하가 줄어 **더 많은 더미(3,000+) 구동 가능**
- **어필 전략:**
  > "이번 테스트의 목적은 지형 연산 부하 측정이 아니라, 대규모 커넥션 상황에서 IOCP 패킷 수신 및 분산 JobQueue 브로드캐스팅 성능을 한계까지 몰아붙이는 것이었습니다. 따라서 의도적으로 장애물 없는 대형 오픈월드 환경을 시뮬레이션해 네트워크 성능 지표 확보에 집중했습니다."
  - → 목적이 뚜렷하면 면접관도 100% 납득

### 🚫 방법 3. [피해야 할 방법] Unity 물리 엔진(NavMesh)을 통째로 서버에 얹기

- C++ 서버에 Unity NavMesh 바이너리를 파싱하는 무거운 외부 라이브러리(Recast/Detour 등)를 신입 단계에서 연동하는 것은 배보다 배꼽이 커진다.
- 구현하다 정작 중요한 IOCP나 DB 연동을 놓치기 십상.
